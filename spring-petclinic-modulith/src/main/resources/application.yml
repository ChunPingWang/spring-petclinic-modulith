# Spring PetClinic Modulith Application Configuration

spring:
  application:
    name: spring-petclinic-modulith

  # AI Configuration (optional - provide API key to enable)
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:dummy}

  # JPA Configuration
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  
  # DataSource Configuration (HSQLDB in-memory by default)
  datasource:
    url: jdbc:hsqldb:mem:petclinic
    username: sa
    password: 
    driver-class-name: org.hsqldb.jdbcDriver
  
  # Initialize database schema and data
  sql:
    init:
      mode: always
      schema-locations: classpath:db/hsqldb/schema.sql
      data-locations: classpath:db/hsqldb/data.sql
  
  # Spring Modulith Configuration
  modulith:
    events:
      # Use JPA-based event publication registry
      republish-outstanding-events-on-restart: true
    observability:
      # Enable observability for module interactions
      enabled: true

# OpenAPI/Swagger Configuration
springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operationsSorter: method
    tagsSorter: alpha
    display-request-duration: true
    show-extensions: true
    doc-expansion: none
  show-actuator: false

# Server Configuration
server:
  port: 8080
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
  
# Management & Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,modulith,env,loggers
      base-path: /actuator
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
    modulith:
      enabled: true
  
  # Health Groups
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
  
  # Metrics
  metrics:
    tags:
      application: ${spring.application.name}
      environment: dev
    distribution:
      percentiles-histogram:
        http.server.requests: true
      slo:
        http.server.requests: 50ms,100ms,200ms,500ms,1s,2s
    export:
      prometheus:
        enabled: true
  
  # Tracing (Zipkin 為選用，使用 docker-compose 時會啟動)
  tracing:
    sampling:
      probability: 1.0
    propagation:
      type: w3c,b3
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_ENDPOINT:http://localhost:9411/api/v2/spans}
  
  # OpenTelemetry
  otlp:
    metrics:
      export:
        enabled: false
        interval: 10000ms

# Logging
logging:
  level:
    root: INFO
    org.springframework.samples.petclinic: DEBUG
    org.springframework.modulith: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

---
# MySQL Profile
spring:
  config:
    activate:
      on-profile: mysql
  datasource:
    url: jdbc:mysql://localhost:3306/petclinic?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: petclinic
    password: petclinic
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:db/mysql/schema.sql
      data-locations: classpath:db/mysql/data.sql

---
# Docker Profile
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:mysql://mysql-server:3306/petclinic?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: petclinic
    password: petclinic
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:db/mysql/schema.sql
      data-locations: classpath:db/mysql/data.sql
