# Spring PetClinic Modulith - 模組文件總覽

**專案**: Spring PetClinic Modulith
**版本**: Phase 16 (六角形架構)
**最後更新**: 2025-11-23

---

## 📚 文件目的

本目錄包含 Spring PetClinic Modulith 專案中每個模組的詳細技術文件。每個模組都有獨立的 README.md 檔案，描述其架構、職責、API、測試策略和未來改進方向。

**遵循憲章原則**: 根據專案憲章第 IX 條（文件與在地化標準），本文件使用**繁體中文 (zh-TW)** 撰寫，以便本地團隊協作。

---

## 🗂️ 模組總覽

Spring PetClinic Modulith 由 **5 個主要模組**組成：

| 模組 | 狀態 | 架構模式 | 測試數量 | 說明 |
|------|------|----------|---------|------|
| [**Customers**](customers/) | ✅ Phase 16 完成 | 六角形架構 | 21 tests | 客戶與寵物管理 |
| [**Vets**](vets/) | ✅ Phase 12 完成 | 六角形架構 | 19 tests | 獸醫管理 |
| [**Visits**](visits/) | ✅ Phase 13 完成 | 六角形架構 | 31 tests | 就診記錄管理 |
| [**GenAI**](genai/) | ⏸️ Legacy 架構 | 整合層 | 3 tests | AI 聊天機器人 |
| [**Shared**](shared/) | ✅ OPEN 模組 | 基礎設施 | N/A | 共用基礎設施 |

**總測試數量**: 71+ 測試（不含 Shared 模組）

---

## 📋 各模組詳細資訊

### 1. Customers 模組 (客戶管理)

**📁 文件**: [customers/README.md](customers/README.md)

**職責**:
- 客戶資訊管理（新增、查詢、更新、刪除）
- 寵物資訊管理
- 寵物類型管理
- 發布領域事件（CustomerCreated, CustomerUpdated, CustomerDeleted, PetAdded, PetDeleted）

**架構特點**:
- ✅ 完整六角形架構（Domain → Business → Infrastructure）
- ✅ Business 層零框架依賴（純 Java）
- ✅ 13 個 Business 層測試
- ✅ 8 個 Service 層測試
- ✅ API 層完整測試覆蓋

**公開 API**:
- `CustomerService` - 客戶管理服務介面
- `Customer` - 客戶領域模型
- 領域事件：CustomerCreated, CustomerUpdated, CustomerDeleted, PetAdded, PetDeleted

**REST 端點**:
- `GET /owners` - 查詢所有客戶
- `GET /owners/{id}` - 查詢單一客戶
- `POST /owners` - 建立新客戶
- `PUT /owners/{id}` - 更新客戶資訊
- `GET /owners/{ownerId}/pets` - 查詢寵物
- `POST /owners/{ownerId}/pets` - 新增寵物

---

### 2. Vets 模組 (獸醫管理)

**📁 文件**: [vets/README.md](vets/README.md)

**職責**:
- 獸醫資訊管理（新增、查詢、更新、刪除）
- 獸醫專業管理
- 發布領域事件（VetCreated, VetUpdated, VetDeleted）

**架構特點**:
- ✅ 完整六角形架構（Domain → Business → Infrastructure）
- ✅ Business 層零框架依賴（純 Java）
- ✅ 12 個 Business 層測試
- ✅ 8 個 Service 層測試
- ✅ 7 個 API 層測試（完整 CRUD）

**公開 API**:
- `VetService` - 獸醫管理服務介面
- `Vet` - 獸醫領域模型
- 領域事件：VetCreated, VetUpdated, VetDeleted

**REST 端點**:
- `GET /vets` - 查詢所有獸醫
- `GET /vets/{id}` - 查詢單一獸醫
- `POST /vets` - 建立新獸醫
- `PUT /vets/{id}` - 更新獸醫資訊
- `DELETE /vets/{id}` - 刪除獸醫

---

### 3. Visits 模組 (就診記錄管理)

**📁 文件**: [visits/README.md](visits/README.md)

**職責**:
- 就診記錄管理（排程、查詢、完成、取消）
- 就診狀態追蹤（SCHEDULED, COMPLETED, CANCELLED）
- 跨模組驗證（驗證寵物與獸醫存在性）
- 發布領域事件（VisitScheduled, VisitCompleted, VisitCancelled）

**架構特點**:
- ✅ 完整六角形架構（Domain → Business → Infrastructure）
- ✅ 使用 Port-Adapter 模式進行跨模組驗證
- ✅ 20 個 Business 層測試（最高測試覆蓋）
- ✅ 11 個 Service 層測試
- ✅ API 層完整測試覆蓋

**公開 API**:
- `VisitService` - 就診管理服務介面
- `Visit` - 就診領域模型
- `VisitStatus` - 就診狀態枚舉
- 領域事件：VisitScheduled, VisitCompleted, VisitCancelled

**REST 端點**:
- `GET /visits` - 查詢所有就診記錄
- `GET /visits/{id}` - 查詢單一就診記錄
- `GET /visits?petId={petId}` - 按寵物查詢
- `GET /visits?vetId={vetId}` - 按獸醫查詢
- `POST /visits` - 排程新就診
- `POST /visits/{id}/complete` - 完成就診
- `POST /visits/{id}/cancel` - 取消就診

**跨模組驗證**:
- `PetValidator` port → `CustomerServicePetValidator` adapter
- `VetValidator` port → `VetServiceVetValidator` adapter

---

### 4. GenAI 模組 (AI 聊天機器人)

**📁 文件**: [genai/README.md](genai/README.md)

**職責**:
- AI 聊天服務（整合 OpenAI API）
- 向量儲存同步（監聽領域事件）
- RAG (Retrieval-Augmented Generation) 實現
- 提供上下文相關的智慧回應

**架構特點**:
- ⏸️ Legacy/整合層架構（有意保持）
- ✅ 使用 Spring AI 框架
- ✅ 非同步事件監聽（鬆耦合）
- ✅ 3 個整合測試

**公開 API**:
- `ChatService` - AI 聊天服務介面
- `ChatRequest` / `ChatResponse` - DTO

**REST 端點**:
- `POST /genai/chat` - 與 AI 聊天

**事件訂閱**:
- `CustomerCreated`, `CustomerUpdated`, `CustomerDeleted` - 同步客戶資料至向量儲存
- `VetCreated`, `VetUpdated`, `VetDeleted` - 同步獸醫資料至向量儲存

**為何不重構**:
- 整合層特性：主要職責是整合外部服務
- 無核心業務邏輯
- AI 技術快速變化，需保持靈活性
- Spring AI 框架已提供足夠抽象

---

### 5. Shared 模組 (共用基礎設施)

**📁 文件**: [shared/README.md](shared/README.md)

**職責**:
- 全域配置管理（Actuator, Metrics, Tracing, OpenAPI）
- 統一例外處理
- 可觀測性基礎設施
- 健康檢查配置

**架構特點**:
- ✅ OPEN 模組（所有模組可存取）
- ✅ 純基礎設施代碼
- ✅ 無業務邏輯

**主要元件**:

**配置類別**:
- `MicrometerMetricsConfig` - Micrometer 指標配置
- `ObservabilityConfig` - 可觀測性配置
- `DistributedTracingConfig` - 分散式追蹤配置
- `ActuatorConfig` - Actuator 端點配置
- `OpenApiConfig` - OpenAPI/Swagger 配置
- `PetClinicHealthIndicator` - 自訂健康檢查

**Web 基礎設施**:
- `RestExceptionHandler` - 全域例外處理器
- `ErrorResponse` - 統一錯誤回應 DTO

**例外類別**:
- `ResourceNotFoundException` - 資源不存在例外

**Actuator 端點**:
- `/actuator/health` - 健康檢查
- `/actuator/info` - 應用程式資訊
- `/actuator/metrics` - 指標查詢
- `/actuator/prometheus` - Prometheus 格式指標
- `/actuator/modulith` - Spring Modulith 模組結構

**OpenAPI 端點**:
- `/swagger-ui.html` - Swagger UI
- `/v3/api-docs` - OpenAPI JSON 規格

---

## 🏗️ 架構模式比較

### 六角形架構模組 (Customers, Vets, Visits)

**層級結構**:
```
Infrastructure Layer (框架依賴)
    ↓ 依賴於 ↓
Business Layer (純 Java - 零框架依賴)
    ↓ 依賴於 ↓
Domain Layer (純 POJO)
```

**優勢**:
- ✅ 業務邏輯與框架完全解耦
- ✅ 100% 純 Java 測試（45+ Business 層測試）
- ✅ 易於測試與維護
- ✅ 框架升級無影響

### 整合層架構 (GenAI)

**結構**:
```
ChatService (公開介面)
    ↓
internal/ (Spring AI 整合實現)
```

**適用場景**:
- 整合外部服務（OpenAI API）
- 無複雜業務邏輯
- 技術快速變化

### OPEN 模組 (Shared)

**特性**:
- 所有模組可直接存取
- 不受 Spring Modulith 邊界限制
- 純基礎設施代碼

---

## 🔗 模組依賴關係

### 依賴圖

```
┌─────────┐
│ Shared  │ ← OPEN 模組（所有模組可存取）
└─────────┘
     ↑
     │
     ├───────────────────────────────┬────────────────┐
     │                               │                │
┌──────────┐    ┌─────────┐    ┌──────────┐    ┌────────┐
│Customers │ ←─ │ Visits  │ ←─ │  GenAI   │    │  Vets  │
└──────────┘    └─────────┘    └──────────┘    └────────┘
     ↑               ↑                ↑              ↑
     └───────────────┴────────────────┴──────────────┘
              (透過事件監聽 - 非同步)
```

### 同步依賴

- **Visits → Customers**: 透過 `PetValidator` port 驗證寵物
- **Visits → Vets**: 透過 `VetValidator` port 驗證獸醫
- **GenAI → Customers**: 呼叫 `CustomerService` 查詢客戶資料
- **GenAI → Vets**: 呼叫 `VetService` 查詢獸醫資料
- **所有模組 → Shared**: 使用共用基礎設施

### 非同步依賴（事件）

- **Customers → GenAI**: 發布 `CustomerCreated`, `CustomerUpdated`, `CustomerDeleted` 事件
- **Vets → GenAI**: 發布 `VetCreated`, `VetUpdated`, `VetDeleted` 事件
- **Visits → (未來)**: 可發布 `VisitScheduled`, `VisitCompleted` 事件

---

## 📊 測試策略總覽

### 測試金字塔

```
              ┌────────────────┐
              │  API 層測試    │  ← REST 端點測試 (MockMvc)
              │    (26 tests)  │
              └────────────────┘
                     ▲
              ┌─────────────────────┐
              │   Service 層測試    │  ← 整合測試 (委派驗證)
              │     (27 tests)      │
              └─────────────────────┘
                       ▲
              ┌──────────────────────────┐
              │    Business 層測試       │  ← 純 Java 單元測試
              │      (45 tests)          │  ← 零框架依賴
              └──────────────────────────┘
```

### 各層測試分佈

| 模組 | Business 層 | Service 層 | API 層 | 總計 |
|------|------------|------------|--------|------|
| Customers | 13 | 8 | - | 21 |
| Vets | 12 | 8 | 7 | 19* |
| Visits | 20 | 11 | - | 31 |
| GenAI | - | 3 | - | 3 |
| **總計** | **45** | **27** | **7+** | **71+** |

*註：Vets 模組的 API 層測試數量為 7 個，包含在 VetResourceTest 中*

### 測試特點

- ✅ **Business 層**: 純 Java，不使用 Spring 上下文（45 個測試）
- ✅ **Service 層**: 驗證委派與模型轉換（27 個測試）
- ✅ **API 層**: 驗證 HTTP 契約與狀態碼
- ✅ **覆蓋率**: CRUD 操作、邊界條件、錯誤處理、跨模組驗證

---

## 📈 效能與可觀測性

### Micrometer 指標

每個模組都使用 `@Timed` 註解記錄效能指標：

**Customers 模組**:
- `petclinic.owner.create`, `petclinic.owner.update`, `petclinic.owner.findAll`
- `petclinic.pet.create`, `petclinic.pet.update`

**Vets 模組**:
- `petclinic.vet.create`, `petclinic.vet.update`, `petclinic.vet.delete`
- `petclinic.vet.findAll`, `petclinic.vet.findById`

**Visits 模組**:
- `petclinic.visit.schedule`, `petclinic.visit.complete`, `petclinic.visit.cancel`
- `petclinic.visit.findByPetId`, `petclinic.visit.findByVetId`

**GenAI 模組**:
- `petclinic.chat.request`, `petclinic.chat.vectorstore.search`, `petclinic.chat.openai.call`

### 分散式追蹤

- OpenTelemetry 自動追蹤所有 HTTP 請求
- Zipkin UI (http://localhost:9411) 提供視覺化追蹤

### 效能目標

- **讀取操作**: < 200ms p95 延遲
- **寫入操作**: < 500ms p95 延遲
- **並發支援**: 1000+ 並發使用者
- **啟動時間**: < 30 秒

---

## 🚀 未來演進

### 短期改進 (1-3 個月)

1. **效能優化**:
   - 加入快取層 (Customers, Vets 模組)
   - 優化資料庫查詢（N+1 問題）
   - 批次操作支援

2. **功能增強**:
   - 搜尋功能（客戶姓名/電話模糊搜尋）
   - 分頁支援（大量資料查詢）
   - 排程管理（獸醫排程整合）

3. **測試增強**:
   - 加入效能測試
   - 加入端對端測試
   - 提升測試覆蓋率至 95%+

### 中期改進 (3-6 個月)

1. **可觀測性**:
   - Grafana Dashboard（預設監控儀表板）
   - 自動告警規則
   - Log Aggregation (ELK/Loki)

2. **安全性**:
   - API 認證/授權
   - 速率限制
   - 資料脫敏

3. **GenAI 增強**:
   - 對話歷史管理
   - 多語言支援
   - 向量儲存升級（生產級資料庫）

### 長期願景 (6-12 個月)

1. **架構演進**:
   - 評估微服務拆分需求
   - 事件溯源 (Event Sourcing)
   - CQRS 模式

2. **AI/ML 整合**:
   - 預測性分析（就診趨勢預測）
   - 推薦系統（獸醫推薦）
   - 異常檢測（健康異常提醒）

---

## 📚 相關資源

### 專案文件

- [**專案 README**](../../README.md) - 專案總覽與快速開始
- [**專案憲章**](../../.specify/memory/constitution.md) - 架構原則與開發標準
- [**架構決策記錄**](../ARCHITECTURE_DECISIONS.md) - ADR 文件
- [**開發者指南**](../DEVELOPER_GUIDE.md) - 開發指南與範例

### 技術文件

- [Spring Modulith 官方文件](https://docs.spring.io/spring-modulith/docs/current/reference/html/)
- [Spring Boot 文件](https://docs.spring.io/spring-boot/docs/current/reference/html/)
- [Spring AI 文件](https://docs.spring.io/spring-ai/reference/)
- [Micrometer 文件](https://micrometer.io/docs)
- [OpenTelemetry 文件](https://opentelemetry.io/docs/)

---

## 🎯 快速導航

### 依角色導航

**業務分析師**:
1. [Customers 模組](customers/) - 了解客戶管理功能
2. [Vets 模組](vets/) - 了解獸醫管理功能
3. [Visits 模組](visits/) - 了解就診流程

**架構師**:
1. [Shared 模組](shared/) - 了解基礎設施架構
2. [Visits 模組](visits/) - 了解跨模組協作範例
3. [專案憲章](../../.specify/memory/constitution.md) - 了解架構原則

**開發者**:
1. [Customers 模組](customers/) - 六角形架構參考實現
2. [Visits 模組](visits/) - Port-Adapter 模式範例
3. [測試策略](#-測試策略總覽) - 測試金字塔實踐

**AI/ML 工程師**:
1. [GenAI 模組](genai/) - AI 整合實現
2. [Shared 模組](shared/) - 事件監聽機制

### 依任務導航

**加入新功能**:
1. 閱讀 [專案憲章](../../.specify/memory/constitution.md)
2. 參考 [Customers 模組](customers/) 架構模式
3. 遵循測試驅動開發（TDD）

**除錯問題**:
1. 檢查 [Shared 模組](shared/) 的日誌配置
2. 使用 Zipkin 追蹤請求路徑
3. 查詢 Prometheus 指標

**效能優化**:
1. 檢視 [效能與可觀測性](#-效能與可觀測性) 章節
2. 分析 Micrometer 指標
3. 參考各模組的效能目標

---

**最後更新**: 2025-11-23
**維護者**: Spring PetClinic Modulith Team
**文件版本**: 1.0.0 (Phase 16)
